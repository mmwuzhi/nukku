# Nukku TODOs

- Media now-playing title can still mismatch the actual audible content, especially when browser audio is detected through CoreAudio while the active tab title points at unrelated content. Treat this as a media-source attribution problem, not a UI rendering issue. Revisit source priority and browser tab matching separately.
